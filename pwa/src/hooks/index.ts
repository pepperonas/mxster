/**
 * Hooks Barrel Export
 * Centralized export for all custom hooks
 */

// Game State Hooks
export { useGameState } from './useGameState'
export { useGameHistory } from './useGameHistory'

// Authentication Hooks
export { useSpotifyAuth } from './useSpotifyAuth'

// Note: Additional hooks will be fully implemented in Phase 5
// - useSpotifyPlayer (Web Playback SDK)
// - useQRScanner (Camera & QR scanning)
// - useBeatAnimator (Beat detection & scheduling)
// - useThreeWave (Three.js particle system)
